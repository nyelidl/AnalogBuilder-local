# ─────────────────────────────────────────────────────────────────────────────
# Analog Designer — LOCAL / COLAB requirements
# ─────────────────────────────────────────────────────────────────────────────

# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.35
streamlit-ketcher>=0.0.1

# ── Chemistry ────────────────────────────────────────────────────────────────
rdkit>=2023.3
gemmi>=0.6
prody>=2.4

# ── Docking ──────────────────────────────────────────────────────────────────
anyonecandock>=1.1.3
meeko>=0.5,<1.0
dimorphite-dl>=1.3

# ── Data / numerics ──────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
Pillow>=10.0
requests>=2.31

# ── Interaction analysis ──────────────────────────────────────────────────────
# Note: plip requires openbabel Python bindings (built from source via SWIG).
# On Colab: system openbabel is installed via apt; the Python package below
# picks it up automatically if SWIG is present.
# If plip install fails, the app falls back to distance-based cIFP.
plip>=2.3.0
lxml>=4.2.1

# ── 3D Viewer ────────────────────────────────────────────────────────────────
py3Dmol>=2.0
# stmol omitted — heavy Jupyter/notebook deps conflict with Colab

# ── 2D Diagrams ──────────────────────────────────────────────────────────────
cairosvg>=2.7.1

# ── ML: ChemBERTa pocket-aware fragment ranking ──────────────────────────────
# torch & torchvision are pre-installed on Colab — only need transformers
transformers>=4.35
# torch>=2.0       # pre-installed on Colab — skip to avoid reinstall
# torchvision      # pre-installed on Colab

# ── ML: ADMET-AI (optional — large download ~500 MB) ─────────────────────────
# Uncomment to enable full Chemprop MPNN ADMET predictions:
# admet-ai
