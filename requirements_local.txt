# ─────────────────────────────────────────────────────────────────────────────
# Analog Designer — LOCAL / COLAB requirements
# Install order matters: openbabel must be installed before plip
# Run: pip install -r requirements_local.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.35
streamlit-ketcher>=0.0.1

# ── Chemistry ────────────────────────────────────────────────────────────────
rdkit>=2023.3
gemmi>=0.6
prody>=2.4

# ── Docking ──────────────────────────────────────────────────────────────────
anyonecandock>=1.1.3
meeko>=0.5,<1.0
dimorphite-dl>=1.3

# ── Data / numerics ──────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
Pillow>=10.0
requests>=2.31

# ── Interaction analysis ──────────────────────────────────────────────────────
# openbabel==3.2.0 has a pre-built manylinux wheel (no SWIG required).
# MUST be installed before plip, otherwise plip tries to build 3.1.1.1 from source.
openbabel==3.2.0
plip==2.3.0
lxml>=4.2.1

# ── 3D Viewer ────────────────────────────────────────────────────────────────
py3Dmol>=2.0

# ── 2D Diagrams ──────────────────────────────────────────────────────────────
cairosvg>=2.7.1

# ── ML: ChemBERTa pocket-aware fragment ranking ──────────────────────────────
transformers>=4.35
# torch and torchvision: pre-installed on Colab (GPU-enabled).
# For local machines without Colab, uncomment:
# torch>=2.0
# torchvision

# ── ML: ADMET-AI (optional — ~500 MB model download) ─────────────────────────
# Uncomment to enable full Chemprop MPNN ADMET predictions:
# admet-ai
